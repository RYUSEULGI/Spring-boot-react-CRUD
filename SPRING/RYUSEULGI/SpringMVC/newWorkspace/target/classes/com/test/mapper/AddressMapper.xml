<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mapper.AddressMapper">
    <select id="list" resultType="Address">
	    select * from ADDRESS order by SEQ desc
	</select>
    <insert id="insert" parameterType="Address">
		insert into ADDRESS(name, addr, rdate) values(#{name}, #{addr}, now())
	</insert>
    <delete id="delete" parameterType="long">
	    delete from ADDRESS where SEQ=#{seq}
	</delete>
<!-- 	<resultMap type="AddressFile" id="AddressFile">
		<result column="seqf" property="seqf"/>
		<result column="ofname" property="ofname"/>
		<result column="sfname" property="sfname"/>
		<result column="fsize" property="fsize"/>
	</resultMap>
	<resultMap type="Address" id="Address">
		<result column="seq" property="seq"/>
		<result column="name" property="name"/>
		<result column="addr" property="addr"/>
		<result column="rdate" property="rdate"/>
		<collection property="addressFile" resultMap="addressFile"></collection>
	</resultMap>
	<select id="select" parameterType="Address" resultMap="Address">
		select a.seq, a.name, a.addr, a.rdate, 
				f.seqf, f.ofname, f.sfname, f.fsize
		from seq a
		join seqf f
		on a.seq = f.seq
		where a.seq = #{seq}	
	</select> -->
</mapper>